name: R4B-Restore Dialogflow CX Agent

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Select environment to restore"
        required: true
        type: choice
        options:
          - Dev
          - QA
          - Prod
      dry_run:
        description: "Run as Dry Run (Terraform Plan only)?"
        required: true
        type: boolean
      git_tag:
        description: "Git tag or commit to checkout"
        required: true

jobs:
  restore-agent:
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment }}

    env:
      PROJECT_ID: ${{ vars.PROJECT_ID }}
      REGION: ${{ vars.REGION }}
      DIALOGFLOW_REGION: ${{ vars.REGION }}
      AGENT_DISPLAY_NAME: ${{ vars.AGENT_DISPLAY_NAME }}
      TOOL_DISPLAY_NAME: ${{ vars.TOOL_DISPLAY_NAME }}
      DATASTORE_ID: ${{ vars.DATASTORE_ID }}
      TF_STATE_BUCKET: ${{ vars.TF_STATE_BUCKET }}

    steps:
      # 1️⃣ Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.git_tag }}

      # 2️⃣ Authenticate to Google Cloud
      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      # 3️⃣ Setup Terraform
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.9.8

      # 4️⃣ Terraform Init
      - name: Terraform Init
        working-directory: teraform
        run: |
          ENVIRONMENT=$(echo "${{ github.event.inputs.environment }}" | tr '[:upper:]' '[:lower:]')
          echo "⚙️ Initializing Terraform backend for environment: $ENVIRONMENT"
          terraform init -reconfigure \
            -backend-config="bucket=${TF_STATE_BUCKET}" \
            -backend-config="prefix=dialogflow-${ENVIRONMENT}-state"

      # 5️⃣ Terraform Plan
      - name: Terraform Plan
        id: tfplan
        working-directory: teraform
        run: |
          echo "🧾 Running Terraform Plan for Dialogflow CX Agent + Tool setup..."
          TF_VARS="\
            -var=project_id=${PROJECT_ID} \
            -var=region=${REGION} \
            -var=dialogflow_region=${DIALOGFLOW_REGION} \
            -var=agent_display_name=${AGENT_DISPLAY_NAME} \
            -var=tool_display_name=${TOOL_DISPLAY_NAME} \
            -var=existing_data_store_id=${DATASTORE_ID} \
          "

          if [ "${{ github.event.inputs.dry_run }}" = "true" ]; then
            echo "🧪 Dry Run mode — Terraform plan only, no apply."
            terraform plan -lock=false -input=false $TF_VARS
          else
            echo "☁️ Apply mode — generating plan."
            terraform plan -out=tfplan.out -lock=true -input=false $TF_VARS
          fi

      # 6️⃣ Terraform Apply (only if not dry_run)
      - name: Terraform Apply
        if: ${{ github.event.inputs.dry_run == 'false' }}
        working-directory: teraform
        run: |
          echo "🚀 Applying Terraform configuration (Dialogflow CX Agent + Tool)..."
          terraform apply -auto-approve tfplan.out

      # 7️⃣ Capture Terraform Outputs
      - name: Capture Terraform Outputs
        if: ${{ github.event.inputs.dry_run == 'false' }}
        id: extract
        working-directory: teraform
        run: |
          echo "📥 Extracting Terraform outputs..."
          terraform output -json > tf-output.json
          AGENT_ID=$(jq -r '.agent_name.value' tf-output.json)
          echo "agent_id=$AGENT_ID" >> $GITHUB_OUTPUT
          echo "✅ Using Agent ID: $AGENT_ID"

      # 8️⃣ Install cxcli
      - name: Install cxcli
        if: ${{ github.event.inputs.dry_run == 'false' }}
        run: |
          echo "⬇️ Installing cxcli..."
          curl -L -o cxcli.tar.gz https://github.com/xavidop/dialogflow-cx-cli/releases/download/v1.239.0/cxcli_Linux_x86_64.tar.gz
          tar -xzf cxcli.tar.gz
          sudo mv cxcli /usr/local/bin/
          cxcli version

      # 9️⃣ Build agent.zip (excluding workflows)
      - name: Build agent zip
        if: ${{ github.event.inputs.dry_run == 'false' }}
        run: |
          echo "📦 Building agent.zip (excluding .github/workflows)..."
          zip -r agent.zip . -x ".github/workflows/*"
          echo "✅ agent.zip created successfully:"
          ls -lh agent.zip

      # 🔟 Restore Dialogflow CX Agent
      - name: Restore Dialogflow CX Agent
        if: ${{ github.event.inputs.dry_run == 'false' }}
        run: |
          echo "🚀 Restoring Dialogflow CX Agent to ${{ github.event.inputs.environment }}"
          echo "📍 PROJECT_ID=${PROJECT_ID}, REGION=${REGION}, AGENT_ID=${{ steps.extract.outputs.agent_id }}"

          cxcli agent restore "${{ steps.extract.outputs.agent_id }}" \
            --project-id "${PROJECT_ID}" \
            --location-id "${REGION}" \
            --input agent.zip \
            --output-format json \
            --verbose
